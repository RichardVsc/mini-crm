services:
  api:
    build: ./crm/api
    ports:
      - "3001:3001"

  web:
    build:
      context: ./crm/web
      args:
        VITE_API_URL: http://localhost:3001
    ports:
      - "8080:80"
    depends_on:
      - api
